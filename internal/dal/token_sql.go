// Code generated by mtgroup-generator.
package dal

import (
	"database/sql"
	"time"
)

// Make sure not to overwrite this file after you generated it because all your edits would be lost!

const (
	sqlGetToken = `
	SELECT
		id,
		expiration_at,
		token,
		type
	FROM
		tokens
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`

	sqlGetMyTokenID = `
	SELECT
		id
	FROM
		tokens
	WHERE
		created_by=:created_by AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted AND
		bound
	`

	sqlAddToken = `
	INSERT INTO tokens(
		expiration_at,
		token,
		type,
		id,
		created_by,
		isolated_entity_id
	) VALUES (
		:expiration_at,
		:token,
		:type,
		:id,
		:created_by,
		:isolated_entity_id
	)
	RETURNING
		id
	`

	sqlBindTokenToProfile = `
	UPDATE
		tokens
	SET
		bound=true
	WHERE
		id=:id AND
		created_by=:created_by AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted AND
		NOT bound
	`

	sqlDeleteToken = `
	UPDATE
		tokens
	SET
		deleted=true,
		deleted_at=:deleted_at,
		deleted_by=:deleted_by
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`

	sqlEditToken = `
	UPDATE
		tokens
	SET
		expiration_at=:expiration_at,
		token=:token,
		type=:type
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`

	sqlListToken = `
	SELECT
		id,
		expiration_at,
		token,
		type
	FROM
		tokens
	WHERE
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`
	sqlListTokenCount = `
	SELECT
		COUNT(*)
	FROM
		tokens
	WHERE
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`
)

type (
	argGetToken struct {
		ID               sql.NullString `db:"id"`
		IsolatedEntityID string         `db:"isolated_entity_id"`
	}

	argGetMyTokenID struct {
		CreatedBy        string `db:"created_by"`
		IsolatedEntityID string `db:"isolated_entity_id"`
	}
	argAddToken struct {
		ID               string     `db:"id"`
		ExpirationAt     *time.Time `db:"expiration_at"`
		Token            string     `db:"token"`
		Type             string     `db:"type"`
		CreatedBy        string     `db:"created_by"`
		IsolatedEntityID string     `db:"isolated_entity_id"`
	}
	argBindTokenToProfile struct {
		ID               string `db:"id"`
		CreatedBy        string `db:"created_by"`
		IsolatedEntityID string `db:"isolated_entity_id"`
	}
	argEditToken struct {
		ID               string     `db:"id"`
		ExpirationAt     *time.Time `db:"expiration_at"`
		Token            string     `db:"token"`
		Type             string     `db:"type"`
		IsolatedEntityID string     `db:"isolated_entity_id"`
	}
	argDeleteToken struct {
		ID               string     `db:"id"`
		DeletedAt        *time.Time `db:"deleted_at"`
		DeletedBy        string     `db:"deleted_by"`
		IsolatedEntityID string     `db:"isolated_entity_id"`
	}
)
