// Code generated by mtgroup-generator.
package dal

import (
	"database/sql"
	"time"
)

// Make sure not to overwrite this file after you generated it because all your edits would be lost!

const (
	sqlGetSession = `
	SELECT
		id,
		active,
		closing_at,
		created_at,
		expiration_at,
		update_at,
		user_id
	FROM
		sessions
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`

	sqlGetMySessionID = `
	SELECT
		id
	FROM
		sessions
	WHERE
		created_by=:created_by AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted AND
		bound
	`

	sqlAddSession = `
	INSERT INTO sessions(
		active,
		closing_at,
		created_at,
		expiration_at,
		update_at,
		user_id,
		id,
		created_by,
		isolated_entity_id
	) VALUES (
		:active,
		:closing_at,
		:created_at,
		:expiration_at,
		:update_at,
		:user_id,
		:id,
		:created_by,
		:isolated_entity_id
	)
	RETURNING
		id
	`

	sqlBindSessionToProfile = `
	UPDATE
		sessions
	SET
		bound=true
	WHERE
		id=:id AND
		created_by=:created_by AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted AND
		NOT bound
	`

	sqlDeleteSession = `
	UPDATE
		sessions
	SET
		deleted=true,
		deleted_at=:deleted_at,
		deleted_by=:deleted_by
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`

	sqlEditSession = `
	UPDATE
		sessions
	SET
		active=:active,
		closing_at=:closing_at,
		expiration_at=:expiration_at,
		update_at=:update_at,
		user_id=:user_id
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`

	sqlListSession = `
	SELECT
		id,
		active,
		closing_at,
		created_at,
		expiration_at,
		update_at,
		user_id
	FROM
		sessions
	WHERE
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`
	sqlListSessionCount = `
	SELECT
		COUNT(*)
	FROM
		sessions
	WHERE
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`

	sqlGetUserForSessionLazyLoading = `
	SELECT
		id,
		expiration_at,
		token,
		type
	FROM
		tokens
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id
	`
)

type (
	argGetSession struct {
		ID               sql.NullString `db:"id"`
		IsolatedEntityID string         `db:"isolated_entity_id"`
	}

	argGetMySessionID struct {
		CreatedBy        string `db:"created_by"`
		IsolatedEntityID string `db:"isolated_entity_id"`
	}
	argAddSession struct {
		ID               string      `db:"id"`
		Active           bool        `db:"active"`
		ClosingAt        *time.Time  `db:"closing_at"`
		CreatedAt        *time.Time  `db:"created_at"`
		ExpirationAt     *time.Time  `db:"expiration_at"`
		UpdateAt         *time.Time  `db:"update_at"`
		UserID           interface{} `db:"user_id"`
		CreatedBy        string      `db:"created_by"`
		IsolatedEntityID string      `db:"isolated_entity_id"`
	}
	argBindSessionToProfile struct {
		ID               string `db:"id"`
		CreatedBy        string `db:"created_by"`
		IsolatedEntityID string `db:"isolated_entity_id"`
	}
	argEditSession struct {
		ID               string      `db:"id"`
		Active           bool        `db:"active"`
		ClosingAt        *time.Time  `db:"closing_at"`
		CreatedAt        *time.Time  `db:"created_at"`
		ExpirationAt     *time.Time  `db:"expiration_at"`
		UpdateAt         *time.Time  `db:"update_at"`
		UserID           interface{} `db:"user_id"`
		IsolatedEntityID string      `db:"isolated_entity_id"`
	}
	argDeleteSession struct {
		ID               string     `db:"id"`
		DeletedAt        *time.Time `db:"deleted_at"`
		DeletedBy        string     `db:"deleted_by"`
		IsolatedEntityID string     `db:"isolated_entity_id"`
	}
)
