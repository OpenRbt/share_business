
// Code generated by mtgroup-generator.
package dal

import (
	"time"
		"database/sql"
)

// Make sure not to overwrite this file after you generated it because all your edits would be lost!

const (

	sqlGetPermission = `
	SELECT
		id,
		name
	FROM
		permissions
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`

	sqlGetMyPermissionID = `
	SELECT
		id
	FROM
		permissions
	WHERE
		created_by=:created_by AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted AND
		bound
	`


	sqlAddPermission = `
	INSERT INTO permissions(
		name,
		id,
		created_by,
		isolated_entity_id
	) VALUES (
		:name,
		:id,
		:created_by,
		:isolated_entity_id
	)
	RETURNING
		id
	`

	sqlBindPermissionToProfile = `
	UPDATE
		permissions
	SET
		bound=true
	WHERE
		id=:id AND
		created_by=:created_by AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted AND
		NOT bound
	`

	sqlDeletePermission = `
	UPDATE
		permissions
	SET
		deleted=true,
		deleted_at=:deleted_at,
		deleted_by=:deleted_by
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`

	sqlEditPermission = `
	UPDATE
		permissions
	SET
		name=:name
	WHERE
		id=:id AND
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`
	
	sqlListPermission = `
	SELECT
		id,
		name
	FROM
		permissions
	WHERE
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`
	sqlListPermissionCount = `
	SELECT
		COUNT(*)
	FROM
		permissions
	WHERE
		isolated_entity_id=:isolated_entity_id AND
		NOT deleted
	`
	
)

type (
    argGetPermission struct {
        ID sql.NullString `db:"id"`
			IsolatedEntityID string `db:"isolated_entity_id"`
    }

	argGetMyPermissionID struct {
			CreatedBy string `db:"created_by"`
			IsolatedEntityID string `db:"isolated_entity_id"`
    }
	argAddPermission struct {
							ID string `db:"id"`
							Name string `db:"name"`
			CreatedBy string `db:"created_by"`
			IsolatedEntityID string `db:"isolated_entity_id"`
	}
	argBindPermissionToProfile struct {
		ID string `db:"id"`
			CreatedBy string `db:"created_by"`
			IsolatedEntityID string `db:"isolated_entity_id"`
	}
	argEditPermission struct {
						ID string `db:"id"`
						Name string `db:"name"`
			IsolatedEntityID string `db:"isolated_entity_id"`
	}
	argDeletePermission struct {
		ID string `db:"id"`
		DeletedAt *time.Time `db:"deleted_at"`
		DeletedBy string `db:"deleted_by"`
			IsolatedEntityID string `db:"isolated_entity_id"`
	}
)